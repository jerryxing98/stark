<div id="div_tagcloud">
    {% for tag in tags %}
        <a href="{{ tag.get_absolute_url }}?username={{ username }}" id="tag-cloud-{{ tag.weight }}">
        {{ tag.name }}
        </a>

    {% endfor %}
</div>